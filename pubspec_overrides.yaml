# Temporary local override for e2e-testing the ticket
# fix_publish_registry_wait (visible, non-hanging registry wait).
# Remove this file and re-run `dart pub get` when done.
#
# gg_publish and gg_test are no longer part of the ticket workspace (they
# were published); the remaining ticket repos still carry path refs to them,
# so pin them to their published versions here.
dependency_overrides:
  gg_multi:
    path: ../gg_multi
  gg_one:
    path: ../gg_one
